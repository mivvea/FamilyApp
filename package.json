{
  "name": "familyapp",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://mivvea.github.io/FamilyApp/",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp src/main.js dist/main.js && cp src/App.css dist/styles.css"
  }
}
